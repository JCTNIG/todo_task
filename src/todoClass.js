export class Todo {
  constructor(title, description, due, id,
  priority, project) {
    this.title = title;
    this.description = description;
    this.due = due;
    this.id = id;
    this.priority = priority
  }

}